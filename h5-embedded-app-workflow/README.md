<div align="center">

# h5-embedded-app-workflow

> *基于现成 H5 内嵌 app 基准项目持续二开的 Claude Code 工作流。*

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Claude Code](https://img.shields.io/badge/Claude%20Code-Skill-blueviolet)](https://claude.ai/code)

<br>

**适用于：已有 H5 内嵌 app 项目作为基准，后续持续导入新的接口文档、新参数、新设计稿并完成开发与模拟验收。**

<br>

这个 Skill 重点解决 4 件事：<br>
基准项目复用、基于接口文档自动识别字段对应关系、Figma 一比一还原，以及最后的模拟测试验收。<br>

[效果示例](#效果示例) · [适用场景](#适用场景) · [工作流](#工作流) · [仓库结构](#仓库结构)

</div>

---

## 效果示例

### 问：这个 H5 项目是旧基准项目，这次导入一套新的接口文档，返回结构差不多，但参数名变了，你直接改好

```text
Claude Code  ❯ 我会先定位旧接口封装和相关页面逻辑，
               再对照导入的接口文档整理请求地址和参数名变化。

               如果返回结构和业务语义保持一致，我会基于接口文档
               在接口层或数据适配层完成对应关系适配，而不是把页面逻辑整体重写。
```

### 问：这是新的 Figma 设计图，按现在的 H5 项目做成一模一样的样式

```text
Claude Code  ❯ 我会先识别当前项目里的布局组件、按钮、表单、卡片和样式变量，
               优先复用现有体系。

               然后按 Figma 的布局、间距、字号、颜色、状态样式做高还原，
               最后检查页面展示和交互一致性。
```

### 问：开发完以后你要自己模拟测试一下，通过测试验收再交给我

```text
Claude Code  ❯ 完成开发后我会补一轮模拟测试：
               包括请求地址、参数映射、页面渲染、表单校验、空态、异常态、
               交互流程和构建/Lint 能力范围内的检查。

               最后会把已验证项、未验证项和建议你手动验收的部分整理出来。
```

---

## 适用场景

这个 Skill 适合以下项目模式：

- 已有一个可运行的 H5 内嵌 app 基准项目
- 后续新需求都在这个基准项目上持续二开
- 业务模型相似，接口返回结构和取值语义大体一致
- 新项目会更换接口地址、请求参数名、页面设计稿
- 需要根据导入的接口文档 + Figma 快速完成一轮可靠开发
- 用户导入完接口文档文件和 Figma 文档后，希望 Claude Code 连续完成全部开发任务

---

## 工作流

这个 Skill 会驱动 Claude Code 执行以下流程：

1. 识别当前基准 H5 项目和可复用模块
2. 对照新需求与旧逻辑，梳理复用点和改动点
3. 导入接口文档，识别地址变化、参数名变化和返回结构关系
4. 基于接口文档在接口层或适配层完成字段对应处理
5. 在接口文档和 Figma 信息齐备后进入连续开发模式，不再反复询问
6. 按 Figma 设计稿高还原实现页面
7. 完成模拟测试与验收清单整理

---

## 设计原则

- **先复用，再修改**：优先基于旧页面和旧逻辑二开
- **先读文档，再适配**：参数名变化优先依据接口文档完成适配
- **先收齐资料，再连续执行**：拿到接口文档和 Figma 后默认一口气做完
- **先局部改，再全局动**：控制改动边界
- **先高还原，再谈优化**：优先满足设计一致性和交付可验收
- **先模拟测试，再交付**：必须给出测试与验收说明

---

## 仓库结构

```text
h5-embedded-app-workflow/
├── README.md
├── LICENSE
├── SKILL.md
├── examples/
│   └── demo-conversation.md
└── references/
    └── design/
        ├── 01-positioning.md
        ├── 02-yapi-and-mapping.md
        └── 03-figma-and-acceptance.md
```

---

## 使用方式

放入 Claude Code 全局 Skill 目录后，在实际项目里直接说：

```text
用 H5 内嵌项目二开工作流帮我做这个需求
```

也可以这样说：

```text
基于当前 H5 项目导入新的接口文档并完成页面修改
这个接口结构一样，但参数名变了，你按旧项目改好
按新的 Figma 设计稿把这个页面一比一做出来，并模拟测试验收
```

---

## 许可证

MIT
