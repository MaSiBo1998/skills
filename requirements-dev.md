---
name: 需求精准开发与安全发布
description: 上游开发链路：接收各形式需求（截图/文字）精准定位并开发代码。强制嗅探项目规范、严控修改边界，完成后自我走查并主动联动下游发布技能。
trigger:
  - 开发需求
  - 改个需求
  - 按照描述开发
  - 修复一下逻辑
  - 根据截图修改
  - 实现图里功能
  - 照着做
---

# 需求精准开发与安全发布

## 功能描述
作为**上游核心需求开发链路**，提供“外科手术式”的安全研发规范：
- **项目规范嗅探**：在动手编码前，必须嗅探当前项目的代码风格、公共组件（如已有的 Button、Modal 等）和命名习惯，确保“入乡随俗”。
- **全景需求解析与边界控制**：解析需求（图文）定位代码。遵守**“红线边界”**：绝不随意重构与本次需求无关的代码，防范副作用。
- **强制代码走查 (Self-Review)**：修改后主动自检（查语法、查引入、查样式冲突），并通过可能存在的 Lint 或测试工具进行校验。
- **顺畅的下游联动**：用户确认走查结果无误后，无缝对接并触发下游 `国家版本发布标签（Git Tag）` 技能。

## 适用场景
- 接收口语化描述的需求新增或 Bug 修复。
- 基于产品给出的带标注截图快速实现功能界面。
- 需要严格控制影响范围的业务逻辑微调任务。

## Trae 自动执行步骤

1. **需求理解与全景定位 (Analysis)**
   - 深度解析用户的需求图片、文字描述或文档片段。
   - 在代码库全局搜索，精准定位要修改的文件与代码行。

2. **环境与项目规范嗅探 (Context)**
   - **强制行为**：扫描项目依赖（`package.json`）和现有代码，识别 UI 框架（如 Vue/React）、CSS 预处理器（Sass/Tailwind）以及项目的公共工具类库（`utils` / `components`）。
   - 确定修改方案，并向用户简述影响范围。

3. **精准编码与边界把控 (Coding)**
   - 根据需求和规范进行针对性开发。
   - **红线原则**：严格执行“只改该改的”，不随意格式化全文件、不优化无关逻辑，保证侵入性最小。

4. **强制走查与自我校验 (Self-Review)**
   - 代码修改完毕后，主动检查引入的变量是否声明、语法是否正确、样式是否越界。
   - （可选）若项目中有 `npm run lint` 或相关语法检查工具，可主动执行以辅助校验。
   - 走查完成后，**主动暂停执行**并向用户提问：“代码已修改并完成自我走查。请亲自确认一下这些改动是否有问题？”
   - 等待用户反馈。若用户指出问题，则定点修复并再次请求确认。

5. **平滑触发下游发布流 (Publish Linkage)**
   - 当用户回复“确认无误”后，**主动提问**：“代码已确认，是否需要立即发布国家版本（例如：发布墨西哥 / 发布哥伦比亚 / 发布尼日利亚）？”
   - 若用户确认发布某个国家，**立即通过 `Skill` 工具无缝调用 `国家版本发布标签（Git Tag）` 技能**，并传入指定国家，执行全自动发布链路。