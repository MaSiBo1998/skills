---
name: 项目一比一自动复刻 (Project Replica)
description: 接收源项目代码（如Flutter/Vue），一比一全自动复刻为目标技术栈（如H5/React）。自动拆解模块并连续开发，无需每次小模块完成都要求确认。
---

# 🎯 核心目标
作为高级架构师和全栈工程师，你的任务是将源项目（如Flutter代码）完美、一比一地复刻到目标技术栈（如React/H5）。
**最核心原则**：制定好拆解计划后，**自动连续完成**所有子模块的开发。期间不要因为完成了一个小组件就停下来询问用户“是否继续”，必须一口气推进，直到整个大模块或整个项目复刻完毕。

# 🔄 执行工作流

### 1. 深度分析与任务拆解 (Analysis & Breakdown)
- 仔细阅读用户提供的源项目代码（分析页面结构、状态管理、路由、API请求、样式特征）。
- 制定详细的复刻计划，并**强制使用 `TodoWrite` 工具**将任务拆解为具体的子任务（如：1. 基础依赖与全局状态配置；2. 路由搭建；3. 公共组件复刻；4. A页面逻辑与UI复刻；5. B页面...）。

### 2. 基础工程与规范对齐 (Scaffolding)
- 检查目标项目环境，按需搭建基础框架、安装必须的依赖（如React Router, Zustand/Redux, Tailwind等）。
- 确立目标项目的目录结构、主题样式变量，确保能完美承接源项目的UI风格。

### 3. 🚀 全自动连续开发循环 (Autonomous Coding Loop)
**（🚨 严格遵守：禁止在每个小任务完成后中断打断用户！）**
- 严格按顺序执行 Todo 列表中的任务。
- 每完成一个模块：
  1. **自我走查**：检查样式变量是否对应，业务逻辑（如点击事件、表单校验）是否一比一还原，绝不遗漏。
  2. **状态更新**：调用工具标记当前 Todo 为已完成 (`completed`)。
  3. **自动推进**：**立即开始**下一个 Todo 任务的开发，**绝对不要**停下来等待用户回复“继续”。
- *阻断处理*：仅在遇到致命错误（如严重依赖冲突、关键源文件缺失）时，才挂起任务并向用户说明情况求助。

### 4. 全局测试与自我校验 (Testing & Self-Check)
- 当所有拆解的 Todo 任务完成后，进行一次全局的代码走查，确保没有遗留的 `// TODO` 占位符或未处理的 Mock 数据。
- 强制进行项目规范嗅探，确保复刻后的代码符合当前仓库的 Lint 规则。

### 5. 验收与平滑触发下游 (Acceptance & Publish Linkage)
- 全部复刻并自测完成后，向用户做最终汇报总结：“所有模块均已自动复刻完成，请验收整体代码。”
- 当用户回复确认无误后，**主动提问**：“项目复刻已确认，是否需要执行提交流程或发布国家版本标签？”
- 若用户确认，**立即通过 `Skill` 工具无缝调用 `国家版本发布标签（Git Tag）` 技能**，执行全自动发布链路。

# ⚠️ 边界与约束 (Constraints)
1. **极简打扰原则**：一旦开始复刻，让代码飞一会儿！利用 Todo 工具让用户感知进度即可，严禁在执行循环中反问用户“接下来做哪个”。
2. **像素级与逻辑级双保真**：UI 样式、交互细节、业务逻辑必须与源项目严格对齐，不可自行阉割功能。若源项目存在明显的 Bug，复刻时应予以修复并添加注释说明。
3. **安全防死循环**：在连续生成代码时，注意控制每次修改的上下文大小，避免大文件一次性写入导致截断。
