---
name: "feishu-bug-handler"
description: "读取并解析飞书提测文档，筛选跟进人为“马嗣博”的bug并自动分析修复。当用户要求处理飞书bug或解决指派给马嗣博的测试问题时触发。"
---

# 飞书提测 Bug 自动处理 (马嗣博专属)

这个 Skill 旨在自动化处理飞书提测文档中指派给“马嗣博”的 Bug。它会引导大模型读取文档、过滤数据、定位本地代码并提供修复方案。

## 触发条件
当用户要求“处理飞书bug”、“解决我的提测bug”或提供飞书测试文档链接/内容时自动调用。

## 核心执行步骤 (Agent 指南)

1. **获取 Bug 数据**：
   - 优先尝试读取用户提供的飞书文档链接。
   - 如果遇到权限拦截无法读取，**必须主动引导**用户将飞书文档内容以文本、Markdown 或 CSV 格式粘贴到对话中，或者提供导出的文件路径。

2. **数据筛选与提取**：
   - 解析获取到的数据列表。
   - 过滤提取出【跟进人】、【指派给】或【处理人】字段值为 **“马嗣博”** 的所有 Bug 记录。
   - 忽略其他人的 Bug，保持专注。

3. **代码定位与分析**：
   - 遍历属于马嗣博的每一个 Bug。
   - 根据 Bug 描述、复现步骤或页面路径，使用 `SearchCodebase`、`Grep` 或 `Glob` 工具在当前项目（`d:\code\front-management`）中检索相关的 Vue 组件、JS/TS 逻辑代码或 API 接口。

4. **代码修复与执行**：
   - 找到目标代码后，分析缺陷原因。
   - 提供具体的代码修复方案。如果是已存在的文件，务必使用 `show_diff` 工具展示修改内容；如果是新文件，输出带绝对路径的 Markdown 代码块（并按要求添加函数级注释）。

5. **总结汇报**：
   - 全部处理完毕后，输出一份简洁的修复报告。
   - 列出“已解决的 Bug 标题”和“涉及修改的文件”，以便用户前往飞书更新 Bug 状态。