interface:
  display_name: "H5 接口映射"
  short_description: "解析接口文档并迁移 H5 API 字段"
  default_prompt: "使用 h5-api-mapping 解析接口文档，输出字段映射表，并迁移 H5 项目的 API 字段；支持同结构混淆字段替换，只替换接口地址、请求头、请求入参、响应字段和全局配置字段，不改业务流程。"
