---
name: h5-official-site
description: H5 官网与挂载页开发。用于官网项目相关需求，包括授权、隐私、贷款、条款协议文档生成 App WebView 可展示的静态 HTML，官网协议入口、隐私/贷款/条款 Tab、线上协议链接 iframe 展示，App 内嵌官网协议/隐私开发问答，以及后续独立小 H5 挂载到官网域名下的开发场景。
---

# H5 官网与挂载页开发

本 skill 负责官网相关多场景任务：协议文档转静态 HTML、官网协议入口/Tab/iframe 展示、App 内嵌官网协议/隐私开发问答，以及独立小 H5 挂载到官网域名下的开发问题。不得擅自改写法律正文。

## 执行方式

1. 判断任务类型：
   - 协议文档转 HTML：收集 4 份本地协议文档、可选协议链接、目标 `public` 目录。
   - 协议展示入口：收集目标路由、默认选中协议、Tab 协议列表、线上协议链接、旧入口兼容要求。
   - App 内嵌开发问答：识别为协议/隐私相关 App WebView、H5 内嵌、原生跳转、链接配置、验收口径问答；需求细节未沉淀前，只做问题归类、读取现有项目实现和风险说明，不编造固定实现规则。
   - 独立小 H5 挂载官网域名：识别为官网域名下新增独立 H5、活动页、工具页、落地页或嵌入页；需求细节未沉淀前，先收集挂载路径、构建产物目录、资源前缀、路由模式和部署方式。
2. 加载 `references/official-site.md`，按其中原流程执行。
3. `.docx` 只允许使用已安装的 `docx` skill 解析。
4. 文档转 HTML 时，链接只用于文件命名，不作为正文来源。
5. 协议展示入口时，优先参考同类项目既有实现，使用 Tab 切换 iframe 展示线上协议链接。
6. App 内嵌开发问答沉淀完成前，若用户给出具体需求，先记录“待沉淀问题、项目事实、最终结论”，交付时建议写回本 skill。
7. 小 H5 挂载规则沉淀完成前，必须基于项目现有官网部署方式和用户提供的域名/路径作答，不预设固定构建或发布方案。

## 约束

- 默认只输出西语正文。
- 不追加原始链接页脚，除非用户明确要求。
- 标题和正文必须拆分为独立标签。
- 不引入外部 JS/CSS/CDN。
- iframe 展示线上协议链接时，不强依赖注入 iframe 内部样式；跨域线上链接在本地开发环境会被浏览器拦截，只控制 iframe 外层容器、标题和 Tab UI。
- 独立小 H5 挂载到官网域名时，优先确认 `base`、资源前缀、路由模式、部署目录、旧入口兼容和移动端 WebView 验收方式。
