---
name: Figma 设计图还原 (Figma to Code)
description: 上游开发链路：调用 Figma MCP 自动生成高保真前端代码。强制执行项目规范嗅探与严格代码走查，无误后主动调用下游发布技能。
trigger:
  - 还原设计稿
  - figma to code
  - 生成 Figma 代码
  - 帮我写一下这个 figma
---

# Figma 设计图还原 (Figma to Code)

## 功能描述
作为**上游核心需求开发链路**，不仅负责代码还原，还确保与现有项目的完美融合：
- **项目规范嗅探**：在写代码前，主动嗅探并对齐当前项目使用的组件库（如 AntD、Element）、样式方案（如 Tailwind、CSS Modules）及命名规范。
- **自动化 Figma 解析**：调用 MCP 提取样式、Auto Layout、文本等全量数据，并映射为结构清晰的语义化代码。
- **代码自我走查 (Review)**：完成后主动查错（如未引用的组件、语法异常），并主动汇报结果请求人工确认。
- **顺畅的下游联动**：走查通过并确认后，直接调用下游 `国家版本发布标签（Git Tag）` 进行自动化发布。

## 适用场景
- 基于 Figma 链接（File URL / Node ID）快速实现局部页面或封装可复用 UI 组件。
- 提取 Design Tokens（颜色、阴影）配置到项目中。

## Trae 自动执行步骤

1. **环境与项目规范嗅探 (Context)**
   - 提取 Figma URL，获取目标节点。
   - **强制嗅探**：扫描项目目录（如 `package.json`、`src/components`），识别框架（Vue/React）与样式工具库，确保生成的代码风格与已有组件一致，而非突兀的“孤岛代码”。

2. **调用 Figma MCP 获取设计数据**
   - 获取目标节点并深度解析文档树。提取布局 (Auto Layout)、样式 (Fills/Strokes)、排版及图片占位符。

3. **高保真编码与写入**
   - 将 Figma 数据转换为 DOM 结构，优先使用 Flexbox 还原 Auto Layout。
   - 提取色值并转换为项目约定格式（如 Tailwind 类名或 CSS 变量）。
   - 将生成的、无语法错误的组件代码写入指定路径（如 `src/components/MyComponent.tsx`）。

4. **强制代码走查与确认 (Review)**
   - 代码生成完毕后，**主动暂停**，进行全面的自我检查（查缺漏、查语法、查引入）。
   - 向用户汇报：“已完成还原和自我走查，这是修改的内容（简要列举）。请确认代码效果是否符合预期？”
   - 根据反馈微调，直至用户确认“无误”。

5. **平滑触发下游发布流 (Publish Linkage)**
   - 用户确认代码通过后，**主动提问**：“是否需要发布国家版本（如墨西哥、哥伦比亚、尼日利亚）？”
   - 若用户确认发布某个国家，**立即通过 `Skill` 工具无缝调用 `国家版本发布标签（Git Tag）` 技能**，并传入用户指定的国家，执行全自动化发布链路。